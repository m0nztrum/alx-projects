#include <stdio.h>
#include <stdlib.h>

/**
 * main -Entry point*
 * Printf: prints the code in the ""
 * Return: (0) a sucess
 */

int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
