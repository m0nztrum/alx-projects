#include "monty.h"

/**
 * nop - function that does nothing.
 * @stack: pointer tothe top of the stack.
 * @line_number: The line number where the operator is called.
 *
 * Return: nothing
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
